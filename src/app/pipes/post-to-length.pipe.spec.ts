import { PostToLengthPipe } from './post-to-length.pipe';

describe('PostToLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new PostToLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
